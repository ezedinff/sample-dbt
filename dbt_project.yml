name: lighthouse_dbt_demo
version: '1.0.0'
config-version: 2

profile: lighthouse_dbt_demo

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]

models:
  lighthouse_dbt_demo:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: analytics
