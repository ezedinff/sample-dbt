# Scenario notes

Scenario → warehouse → model mapping for demos lives in the Lighthouse
operator docs (`tenant-context/example/`), not in this client repo.

This project only contains production-shaped dbt models. Do not encode
scenario names, expected fixes, or investigation hints in SQL or YAML here.
